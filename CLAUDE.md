# 酒馆助手前端界面或脚本编写

## 最高约束:代码注释只留最必要的功能说明

解释、自辩、设计理由、开发者旁白、历史沿革、实测读数一律不写。
位阶高于本文件其余各条,也高于「我觉得这条值得留个说明」。

## 代码注释纪律(硬规,反复犯过)

**注释只写「读代码看不出来的那一句」。** 一处一行为限,写完问一句:
"这句是给**代码**的,还是给**审阅我的人**的?" 后者一律删。

**只许写这四类**:

1. 非显然的约束(`z 必须 >5,否则被金线横穿`)
2. 外部契约与坑(宿主 sanitizer / iframe rAF 被暂停 / `box-shadow` 任一项写 `none` 整条失效)
3. 反直觉的取值(`留 .8% 缝宽,纯 0 会被判成空裁剪区不画`)
4. 规范索引(`§9.3 相位 .15`)—— 一个词,不展开

**一律不许写进代码**(这些去 `地图美化设计.md` / `地图计划.md`):

- 历史沿革:"原来是 X,已改成 Y""这条从步1 藏到步7"
- 自辩:"为什么不用另一种写法""为什么幅度写在这儿不写在那儿""必然会 XX"
- 实测数字与验收读数:`802.9↔801.8`、`实测 27 点里只有 3 点`、对比度比值
- 位阶引用、用户原话、变更记录、"这条最贵"这类强调
- 把规范条文整段抄进来(规范在文档里,抄一遍就是第二份可信源,必然漂移)

**判据**:注释体积应该跟着**代码复杂度**长,不跟着**我踩坑的次数**长。
每个坑都在原地立一块碑 ⇒ 注释比代码多,那不是谨慎,是把审计日志倒进了源文件。

@.cursor/rules/项目基本概念.mdc
@.cursor/rules/mcp.mdc
@.cursor/rules/酒馆变量.mdc
@.cursor/rules/酒馆助手接口.mdc
@.cursor/rules/前端界面.mdc
@.cursor/rules/脚本.mdc
@.cursor/rules/mvu变量框架.mdc
@.cursor/rules/mvu角色卡.mdc
