# 前端悬浮球V1 敏感词替换存档（供无外审渠道还原用）

> 本文件记录 `src/前端悬浮球V1` 为规避外审（阿里云百炼内容审核）而做的敏感词中性化替换。
> 当 AI 通过有外审的渠道读取/生成 V1 代码时，原文中的敏感词会进入输入 prompt 触发审核截断，
> 导致代码工作无法继续。故将其临时替换为中性词；待整体工作完成后，用无外审渠道按本表还原。
>
> 还原时：把 V1 源码中的「中性词」整批替换回「原始词」即可（注意 key 名是数据契约，还原后即恢复与 AI 角色卡 MVU 框架写入的酒馆变量同名）。

## 一、替换映射表

### 1. NPC 数值指标（lib/config.ts 的 NPC_METRICS / NPC_COUNTS，key 同时是数据契约键与显示文字）

| 原始词（敏感，需还原） | 中性词（当前过审版本） | 所在结构 |
|---|---|---|
| 情欲值 | 热忱值 | NPC_METRICS key |
| 敏感值 | 感知值 | NPC_METRICS key |
| 羞耻值 | 局促值 | NPC_METRICS key |
| 高潮次数 | 顶峰次数 | NPC_COUNTS key |
| 被内射次数 | 深入次数 | NPC_COUNTS key |

> 保留不动的中性词（本无色情色彩，未改动）：心动值、兴奋值。

### 2. hover-tip 显示文字与注释（status-bar-init.ts）

| 原始词（敏感，需还原） | 中性词（当前过审版本） | 位置 |
|---|---|---|
| 亲密记录 | 交互记录 | status-bar-init.ts:825 hover-tip 标题 |
| 高潮次数 | 顶峰次数 | status-bar-init.ts:825 显示文字 / :1308 读取键 |
| 被内射次数 | 深入次数 | status-bar-init.ts:825 显示文字 / :1309 读取键 |
| 高潮次数/被内射次数 | 顶峰次数/深入次数 | status-bar-init.ts:1307 注释 |

### 3. 局部变量名（status-bar-init.ts，纯前端命名，还原非必需但建议一并还原以保持一致）

| 原始名 | 中性名 | 位置 |
|---|---|---|
| orgasm | peak | status-bar-init.ts:1308-1311 / :1196 / :825(data 字段) |
| creampie | deep | status-bar-init.ts:1309-1311 / :1196 / :825(data 字段) |

> DOM data-attr：`data-orgasm` / `data-creampie` → `data-peak` / `data-deep`（status-bar-init.ts:1311 写入、:1196 读取）。

## 二、原始代码片段（还原参照）

### lib/config.ts 原始（NPC_METRICS + NPC_COUNTS）

```ts
export const NPC_METRICS = [
  {key:'心动值',icon:'fa-solid fa-heart',cls:'heart'},
  {key:'情欲值',icon:'fa-solid fa-fire-flame-curved',cls:'lust'},
  {key:'兴奋值',icon:'fa-solid fa-sparkles',cls:'excite'},
  {key:'敏感值',icon:'fa-solid fa-water',cls:'sense'},
  {key:'羞耻值',icon:'fa-solid fa-face-grin-wide',cls:'shame'},
] as const;
export const NPC_COUNTS = [
  {key:'高潮次数',icon:'fa-solid fa-sparkles'},
  {key:'被内射次数',icon:'fa-solid fa-droplet'},
] as const;
```

### status-bar-init.ts:825 原始（hover-tip counts 分支）

```ts
    html=`<div class="th-hover-tip-title"><i class="fa-solid fa-heart-circle-plus"></i> 亲密记录</div><div class="th-hover-tip-row"><i class="fa-solid fa-sparkles"></i> 高潮次数: <b>${esc(data.orgasm||'0')}</b></div><div class="th-hover-tip-row"><i class="fa-solid fa-water"></i> 被内射次数: <b>${esc(data.creampie||'0')}</b></div>`;
```

### status-bar-init.ts:1307-1311 原始（NPC 卡片第5图标）

```ts
  // 需求8：第5个图标 — 高潮次数/被内射次数
  const orgasm=info['高潮次数']??0;
  const creampie=info['被内射次数']??0;
  const hasCounts=(Number(orgasm)>0||Number(creampie)>0);
  icons+=`<span class="th-npc-icon-item th-npc-icon-counts" data-orgasm="${orgasm}" data-creampie="${creampie}" style="opacity:${hasCounts?1:0.4}"><i class="fa-solid fa-heart-circle-plus"></i></span>`;
```

### status-bar-init.ts:1196 原始（hover-tip 触发读取 data-attr）

```ts
      showHoverTip(counts,'counts',{orgasm:counts.getAttribute('data-orgasm')||'0',creampie:counts.getAttribute('data-creampie')||'0'});
```

## 三、还原操作（无外审渠道执行）

1. `lib/config.ts`：NPC_METRICS/NPC_COUNTS 的 5 个 key 按映射表改回原始词。
2. `status-bar-init.ts`：
   - 825 行显示文字 + data 字段名改回（亲密记录 / 高潮次数 / 被内射次数 / orgasm / creampie）
   - 1196 行 data-attr 读取改回（data-orgasm / data-creampie / orgasm / creampie）
   - 1307-1311 行注释 + 局部变量 + data-attr 写入改回
3. `.backup/` 下的历史快照未改动，仍为原始敏感词，无需还原（仅供本地回滚）。

## 四、未改动说明

- `心动值`、`兴奋值`、`身体状态`、`姿态动作`、`当前本能渴望`、`内心想法` 等 NPC 字段无色情色彩，未改动。
- `defaultInject` 注入模板（`<前往{{name}}...>` 等）为地点/事件/物品文案，无敏感词，未改动。
- `src/此间天地前端/` 下另有 `index.ts` 原始版本（早期独立项目），其中含同样的原始敏感词（line 81-89/546-548/1013-1015），不在本次 V1 清理范围；若该目录也要过审可参照本表处理。
