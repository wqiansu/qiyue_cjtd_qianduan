# 前端悬浮球V1 · 全量代码审查待办（2026-08-16）

> 6 路并行只读审计 + 静态基线全部完成。2026-08-17 已按开干顺序逐条实施并通过验收（tsc 0 错 / prod 构建过 / check:cdn 过）。勾选＝已销项；未勾＝存疑或用户定夺项。
>
> **浏览器实测（2026-08-17 晚）：** 经 `pnpm watch` 实时构建接入已打开的酒馆（127.0.0.1:8000）逐项实机验收，全部通过：运行时 `getWorldStorageKeys` 已含 zui/wstate/theaterCfg/wstateCfg + mem 前缀扫描；world-state 设置注入开关（th-ws-s-inject）接线往返写入 `injectOn`；通话设置四板块（wb-sync/api-plan/记忆/楼层自动触发）齐全；日记记忆入口（openSessionMemory+appMemPanelHtml）到位；美团无「启用会话记忆」死开关且「同步到世界书」保留；crop-simple 图标渲染 SVG 无白块；345 模板黑话复扫仅剩 forum/browser 两处「楼层」经核实为论坛语义（非酒馆黑话）判不改；console 无脚本运行时错误。
> **实测新发现：** `wstate.seed` 模板正文「绑定世界书原文」为黑话漏网（世界书≒酒馆技术术语，非卡专名）——**已修**为「绑定设定原文」；`wplace.advance` 输出示例含「凝霜宫/唐紫苏」卡专名（wplace 非 PROGRESS:120 明列豁免，但属世界态地点子维度）——**留用户定夺**。

## 结论摘要

核心机制与文档契约基本成立：20 app 全部 `registerWorldApp` 自注册、破限除 evolution 两处漏线外均接上、MVU 写 0、内联 onclick / 原生弹窗 0、`chatGenerate` / `inject-plan` / `wb-sync` 链路结构正确。**真正值得动手的是「整包导出会丢数据(P0)」与「发给 AI 的正文混进黑话/专名」两类**，其余多为死配置与图标管线。

验收基线（2026-08-17）：`tsc --noEmit` 0 错；`webpack --mode production` 成功；`check:cdn` 通过（264 导入名齐全）；`dist/前端悬浮球V1/index.js` 产物含全部新改动。

---

## P0 · 导出即丢数据（建议第一优先修）

- [x] **1. 整包导出漏 key** — [lib/world/world-store.ts:205-216](lib/world/world-store.ts#L205-L216)
  `getWorldStorageKeys()` 用字符串字面量，漏掉 `WORLD_LS_KEYS` 已定义的：
  - 角色记忆池 `_th_world_pool_index_v1` + `_th_world_pool_*`（前缀扫描只扫 `_th_world_mem_`，未扫 pool）→ 导入导出丢**全部跨 app 共享 CharPool**
  - `_th_world_zui_v1`（最右数据）、`_th_world_wstate_v1`（结构化世界态数据）
  已核实导出唯一来源链 [modules/init-manager.ts:817-820](modules/init-manager.ts#L817-L820)（`INIT_LS_KEYS + PACK_EXTRA_KEYS + getWorldStorageKeys()`），无其它兜底。
  **已修**：`getWorldStorageKeys` 补入 `zui`/`wstate`/`theaterCfg`/`wstateCfg`（引 `WORLD_LS_KEYS` 成员）+ `poolPrefix` 前缀扫描；顺带登记 theater/wstate 两个独立配置 key。

## P1 · 功能/契约破坏（建议修）

- [x] **2. evolution 涟漪漏破限** — [modules/world/evolution.ts:1467](modules/world/evolution.ts#L1467) / [1513](modules/world/evolution.ts#L1513)
  两条「涟漪外溢生成微信消息」`chatGenerate` 未传 `jailbreak`（同文件其余 5 处均传）→ 这两条破限没接上。
  **已修**：两处补 `jailbreak: evoJailbreak()`。

- [x] **3. json_schema 契约违背** — [modules/ai-summarize.ts:910](modules/ai-summarize.ts#L910)
  传裸 JSON Schema 对象；`@types/function/generate.d.ts:432-441` 的 JsonSchema 要求 `{name, value, ...}` 包裹；`genCfg` 为 `any` 逃过编译。
  **已修**：ai-summarize 直调点补 `{name:'ai_summary', value}`；ai-chat `chatGenerate` 中央对裸 schema 自动补 `{name:'output', value}`（sessionReply/groupReply 全链路覆盖）。

## P2 · 接线断 / 死配置 / 禁词漏网

### 导出/备份族（同 P0 病根，优先级低于 P0）
- [x] **4. PACK_EXTRA_KEYS 漏 key** — [modules/init-manager.ts:796](modules/init-manager.ts#L796)
  漏 `_th_ai_taskpool_v1` / `_th_ai_snapshots_v1`（config.ts:112-113）/ `_th_ai_last_prompt_v1` / `_th_ai_last_drill_v1` → 迁移丢 AI 总结任务池与回滚快照。
  **已修**：补入 `aiTaskpool`/`aiSnapshots`/`aiLastPrompt`/`aiLastDrill` 四 key。
- [x] **5. 基础备份漏 key** — [modules/init-manager.ts:448](modules/init-manager.ts#L448) + [lib/config.ts:103](lib/config.ts#L103)
  基础备份 `exportInitBackup` 漏 `_th_stash_kinds_v1`；`_th_init_cards_v1`（初始卡片中间层）未登记 INIT_LS_KEYS，基础备份/重置不读写它。
  **已修**：config.ts 登记 `initCards`；export/restore 双向补 `stashKinds` + `initCards`。
- [x] **6. 两配置 key 未登记未导出** — [lib/world/theater-store.ts:82](lib/world/theater-store.ts#L82) / [lib/world/world-state-store.ts:174](lib/world/world-state-store.ts#L174)
  `_th_world_theater_cfg_v1` / `_th_world_wstate_config_v1` 不在 WORLD_LS_KEYS 也未进导出 → 玩家配置随整包导入丢失。
  **已修**：登记 `WORLD_LS_KEYS.theaterCfg`/`wstateCfg` 并纳入导出列表。

### 发送给 AI 的正文 · 黑话/专名禁词族（多 agent 交叉命中，系统性）
- [x] **7. chatGenerate 集中注入含黑话** — [lib/world/ai-chat.ts:109](lib/world/ai-chat.ts#L109) / [136](lib/world/ai-chat.ts#L136)
  每一条生成集中注入「【绑定世界书条目（背景设定/本 app 设定，参考勿复述）】」——含黑话「世界书」+ 括号元点评。
  **已修**：统一替换为「【本作背景设定，供参考界定，勿逐字复述】」等三档中立文案。
- [x] **8. 禁词个例**（逐条改字/改标签）
  - modules/world/tangxin.ts:528、modules/world/wechat.ts:564「【世界书/角色书信息】」标签进 AI 正文
  - modules/world/zui.ts:116「万花镜当红」（worldBlock 标签）；modules/world/weibo.ts:215/221「霜月仙宫/仙主」（desc 文案）
  - lib/world/evolution-presets.ts:52-103 12 题材 backdrop「以绑定世界书为准」（黑话无豁免；evolution 只豁免卡专名）
  - [x] 8a. tangxin/wechat；[x] 8b. zui/weibo；[x] 8c. evolution-presets
  **已修**：tangxin/wechat/weibo「世界书/角色书信息」→「本作背景设定…」；zui「万花镜当红」→「当下热榜」；weibo desc/default 去「霜月仙宫/仙主」；evolution-presets「以绑定世界书为准」→「以绑定设定为准」；同族复核又发现 wechat:1590/2220、weibo:495、cal:286、xmly:106、world-genres:134 一并改净（wechat 走 replace 全文件）。
- [ ] **9. （备选）全局黑话 sanitizer** — 发送前统一替换 AI 正文「世界书/角色卡/楼层/破限/注入」等词
  **警告**：盲替换可能误伤，须先逐条审计再上；默认不做、只作选项（对应 README「先只读审计再富化」）。
  **本次未做**（维持逐条改净路线）。

### 死配置 / 未接线开关
- [x] **10. meituan 死开关** — [modules/world/meituan.ts:701](modules/world/meituan.ts#L701) / [1063](modules/world/meituan.ts#L1063)
  `memoryEnabled`「启用会话记忆」只写（渲染+保存）不读，生成链路不消费。
  **已修**：删除死开关（保留有真实消费者的 syncEnabled）。
- [x] **11. world-state 注入死路径** — [modules/world/world-state-ui.ts:528](modules/world/world-state-ui.ts#L528)
  `th-ws-s-inject` handler 无对应 UI 元素 → `injectOn` 永不可从 UI 打开，`refreshWorldInject` 持久注入实际死。
  **已修**：设置面板补 `th-ws-s-inject` 复选框（接线持久注入）。
- [x] **12. queueSysInject 非逐 app 隔离** — [lib/world/ai-chat.ts:15-23](lib/world/ai-chat.ts#L15-L23)
  单条全局队列；8+ app 在 open 时压入，若期间其它 app 先跑 chatGenerate 即串台注入。契约要求逐 app 隔离。
  **已修**：改 per-app Map（`queueSysInject(appId, body)`），chatGenerate 按 appId 消费；6 个调用点全更新。

### §6B 设置缺项
- [x] **13. call.ts 缺组** — [modules/world/call.ts](modules/world/call.ts)（PROGRESS 已明列「通话还缺」）
  无 wb-sync / 无 api-plan 面板 / 无 `openSessionMemory` 入口 / 无 `registerAutoAgent` 自动触发。
  **已修**：补 wb-sync 面板（hangup 时 runMemorySync 沉淀）+ api-plan 面板 + 记忆入口（openSessionMemory + appMemPanelHtml）+ 自动触发（registerAutoAgent，每 N 楼随机未接来电）。call-store 加 `autoInterval`/`lastFloor` 字段。
- [x] **14. diary.ts 缺记忆入口** — [modules/world/diary.ts](modules/world/diary.ts)（cal/theater/xmly/fanfan 均有）
  无 `appMemPanelHtml` / `openSessionMemory`。
  **已修**：data 兜底分支补记忆区块（openSessionMemory + appMemPanelHtml）。
- [ ] **15. wkb.ts 缺组** — [modules/world/wkb.ts](modules/world/wkb.ts)
  无 wb-sync / api / mem / auto 触发；有铁律豁免正当性（不碰世界书/变量、唯一出口塞输入框），按契约照报，处置由用户定。
  **本次未改**（用户定夺：工作台是「通用 AI 造物机」，塞输入框即唯一出口，豁免合理可接受）。
- [ ] **16. places 复选未落实** — [modules/world/places-ui.ts:160](modules/world/places-ui.ts#L160)
  文档主张「地点每个绑多条」改复选，现多选实为「每个条目各建一个地点」（`addPlace(entry)`），单地点仍单 `wbKey`。
  **本次未改**（数据模型改造 `wbKey`→`wbKeys`，有迁移风险，用户定夺）。

### 图标 / 样式管线
- [x] **17. fa-crop-simple 缺注册** — [modules/world/settings-app.ts:223](modules/world/settings-app.ts#L223)
  `iconHtml('fa-crop-simple')` 在 lib/icons.ts 无映射（已核实，lucide-static 有 Crop 可补）→ 「读取管理·提取标签」标题渲染 `.th-ico-missing` 白块。
  **已修**：icons.ts 补 import Crop + `'crop-simple': inner(Crop)`。
- [x] **18. stripFa 只匹配恰好两 fa- 类** — [lib/icons.ts:609](lib/icons.ts#L609)
  - lib/world/wb-picker.ts:51/52/64 带第三类工具类（th-wbp-caret 等）不替换，隐式依赖父页 FontAwesome
  - modules 内 `fa-solid fa-spinner fa-spin`（api-settings.ts:123、managed-modal.ts:386/390、stash-modal.ts:531、init-manager.ts:265）逃出正则，渲染裸 `<i>`
  **已修**：stripFa 改为「含 fa- 前缀类即处理」，取首个非修饰性 fa- 类，未知映射保留原 `<i>`（防白块回归）。

## P3 · 小问题 / 清理（可选批量）

- [x] **19. 死导出**：lib/char-book.ts:32 `hasCharBook` 已删（另两个实际有内部引用：`diffStatData` 用于 341 行、`migrateManagedItem` 用于 28/65 行——审计误报，保留）。
- [x] **20. 死桥 `__deleteAvatar__`** — [status-bar-init.ts:2254-2258](status-bar-init.ts#L2254-L2258) 已删（window + parent 两处）；保留有真实消费者的 `__uploadTarget__`。
- [x] **21. tsc 死变量** — [status-bar-init.ts:198](status-bar-init.ts#L198) `roleAvatarUrl` 未用，已删（`getAvatarFullUrl` 用字串替换完成缩略→原图）→ **tsc 归零**。
- [ ] **22. tags 截断丢标签** — [modules/init-visual-editor.ts:493](modules/init-visual-editor.ts#L493) `collectFromDom` 只取 `tags[0]`；[lib/managed-store.ts:237/245](lib/managed-store.ts#L237) `setItemTags/addItemTag` 硬截单元素（若一卡一标签为设计则忽略，存疑）
  **本次未改**（每卡一标签为既有数据模型约束，非截断 bug）。
- [x] **23. ai-summarize 清空过宽** — [modules/ai-summarize.ts:1263](modules/ai-summarize.ts#L1263) doInject 后 `clearTasks()` 连未勾选项清空（需重跑）
  **已修**：改为按本轮 `taskIds` 逐条 `removeTask`，其它排队任务保留。
- [x] **24. tag-manager 堆积 Modal2** — [modules/tag-manager.ts:151](modules/tag-manager.ts#L151) 删/改后重开无 replace，反复刷新堆栈
  **已修**：openModal2 加 `{ replace: true }`。
- [ ] **25. 折叠箭头死代码** — [status-bar-init.ts:2066-2067](status-bar-init.ts#L2066-L2067) 查询 `.fa-chevron-up` 已被 stripFa 替换为 SVG，永不命中；`outerHTML=` 走 outerHTML 绕过 innerHTML 拦截器
  **本次未改**（存疑：折叠箭头显示可能与 stripFa 时序/局部不替换相关，需实测）。
- [ ] **26. `th-world-active` 清理缺口** — [status-bar-init.ts:1168-1173](status-bar-init.ts#L1168-L1173) closeModal2 无 revive 分支不清；[2405-2430](status-bar-init.ts#L2405-L2430) destroy() 不移除 → 世界开着重载遗留中和态（均存疑，世界壳恒传 revive）
  **本次未改**（存疑）。
- [ ] **27. 遗留外部 toastr / 原生 confirm** —
  - modules/world/tangxin.ts:76-78 全局 toast 走 `(getRoot()).toastr[kind]`（41 处调用），未接 ui-kit thToast，违 §6C
  - modules/world/call.ts:290 `toastr.error`、:403 原生 `confirm`；modules/world/world-app.ts:164 `window.toastr.error`
  **本次未改**（风险项：tangxin 41 处 + call 行为迁移需实测，留验收后由用户定）。
- [x] **28. `_th_custom_tokens` 未纳导出** — [modules/managed-modal.ts:1592](modules/managed-modal.ts#L1592)
  **已修**：补入 PACK_EXTRA_KEYS。
- [x] **29. wkb 直连 generateRaw** — [lib/world/world-api.ts:167-175](lib/world/world-api.ts#L167-L175) 唯一绕过 chatGenerate 直调点（连接测试用途，非内容生成，按设计照报）→ 不涉事。

## 存疑待核（未下结论，开干时先查）

- [x] **30. AI 正文黑话 syстемное 复核** — 已 grep 全 modules/world + lib/world 发往 AI 的模板，除清单所列还发现 wechat:1590/2220、weibo:495、cal:286、xmly:106、world-genres:134 同类漏网，已一并改净；forum-presets 满布卡专名（仙宫/仙主/听风宫/飞雪宫/万花镜）审计清单未列，**新发现**，留用户定夺。
- [ ] **31. cal 节日表** — [modules/world/cal.ts](modules/world/cal.ts)（存疑未动）
- [ ] **32. xmly「万花镜当红」** — [modules/world/xmly.ts:106](modules/world/xmly.ts#L106) 已改「当下热榜」；语义与 zui 同源，销项。
- [ ] **33. map-modal 写 MVU** — 存疑未动。
- [x] **34. 文档漂移两处** —
  - PROGRESS.md §9 红点即时刷：`refreshWorldUnread`/syncdot 为特意移除的 no-op，非缺陷（维持）。
  - lib/world/world-prompts.ts:119 注释与实现矛盾（实现=未知 {{key}} 抹空，注释称原样保留）——**已改注释**「缺失抹空，未知占位符留痕」。

## 开干顺序建议

1. **P0-1** 整包导出漏 key（改 world-store úausführung + init-manager 检查）→ 顺带 P2-4/5/6 导出族
2. **P1-2** evolution 补破限；P1-3 json_schema 核签
3. **P2 禁词族**：7 集中注入改词 + 8a/b/c 逐条（需先跑 30 复核防漏）
4. **P2 死配置**：10/11 由用户定接线或删；12 queueSysInject 隔离
5. **P2 设置缺项**：13/14/15/16（13 最大件，参考其它 app 的 wbSyncPanel）
6. **P2 图标**：17 注册 Crop；18 stripFa 正则放宽
7. P3 批量小修 + tsc 归零
8. 每批后：`npx tsc --noEmit -p tsconfig.json`（0 容忍）+ `pnpm build`（production）+ `pnpm check:cdn`，UI 行为交用户导入 `dist/前端悬浮球V1/index.js` 实测